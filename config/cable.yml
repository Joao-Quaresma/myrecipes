development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  url: <%= ENV["REDIS_URL"] %>
  config.action_cable.url = "wss://#{ENV['RAILS_HOST']}/cable"
  channel_prefix: myrecipes_production
